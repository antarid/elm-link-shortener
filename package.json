{
  "devDependencies": {
    "@testing-library/cypress": "^7.0.1",
    "cypress": "^5.3.0",
    "prettier": "^2.1.2",
    "prettier-plugin-elm": "^0.7.0"
  },
  "scripts": {
    "test:e2e": "cypress run",
    "test:unit": "elm-app test",
    "test": "npm run test:e2e && npm run test:unit"
  }
}
